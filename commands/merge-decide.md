---
description: Approve, reject, or reverse memory merge proposals
---

Make decisions on memory merge proposals. Use the `action` parameter to select the operation.

**Actions:**
- `approve`: Execute a merge proposal to consolidate memories
- `reject`: Decline a merge proposal and keep memories separate
- `reverse`: Undo a previously completed merge and restore original memories
