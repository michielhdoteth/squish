{
  "mcpServers": {
    "squish": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/dist/index.js"],
      "env": {
        "SQUISH_MODE": "local",
        "NODE_ENV": "production",
        "SQUISH_DATA_DIR": "${CLAUDE_PLUGIN_ROOT}/.squish"
      }
    }
  }
}
